<%= form_for @user do |f| %>
  <div class="form">
    <% if @user.errors.any? %>
      <div id="error_explanation">
        <h2>
          <%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:
        </h2>
  
        <ul>
        <% @user.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>
  <div class="row form-group">
    <div class="form-group form-group__text">
      <%= f.label :name %>
      <%= f.text_field :name, class: "form-control" %>
    </div>
    <div class="form-group form-group__text">
      <%= f.label :Username %>
      <%= f.text_field :Username, class: "form-control" %>
    </div>
  
    <div class="form-group form-group__text">
      <%= f.label :password %>
      <%= f.password_field :password, class: "form-control" %>
    </div>
  </div>
<div class="form-group check_boxes">
    <%= f.collection_check_boxes(:ccg_ids, Ccg.all, :id, :name) do |c| %>
      
    <%= c.label(class: "ccg_label") do %>
      
      <%= c.check_box(class: "check_box") %>
      <%= c.object.name %>
        
         
    <% end %>
      <%end%>
      </div>
    <div class="actions">
      <%= f.submit class: "btn btn__dark btn__lg" %>
    </div>
    
  </div>
  <% end %>