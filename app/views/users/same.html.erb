<%current_user.ccgs.each do |ccg|%>
    <%if current_user.samegame(ccg).count > 0%>
        <%=ccg.name%>
        <%@users = current_user.samegame(ccg)%>
        <%= render partial: 'users', locals: {users:@users} %>
    <%end%>
<%end%>